{
  "name": "demo-project",
  "version": "1.0.0",
  "description": "Demo project for job candidates.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json"
  },
  "author": "Ivan Kovic",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
